<!doctype html>
<html lang="en">
  <head>
    <%- include("header")%>
  </head>
  <body>
    <%- include("navbar") %>
    
    <div class='container'>
      <% if (!username) { %>
        尚未登入，請登入
      <% } else { %>
         hello <%= username %>
         <a href='/new_post'>NEW POST</a>

         <div>
            <% for(var i=0; i<posts.length; i++ ) { %>
              <div>
                  <h2><%= posts[i].title %></h2>
                  <p><%= posts[i].content %></p>
              </div>
            <% } %>
         </div>
      <% } %>
    </div>

    <%- include("scripts")%>
  </body>
</html>